{
  "name": "lab-requests-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgCyan.bold\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgCyan.bold\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "server": "npm --prefix backend run dev",
    "backend": "npm --prefix backend run dev",
    "client": "npm --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build",
    "seed": "npm --prefix backend run seed",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^7.1.0",
    "mongoose": "^8.23.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
